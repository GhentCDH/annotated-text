/**
 * DI tokens for adapters.
 * These are in a separate file to avoid circular dependencies.
 */

export const TextAdapterToken = 'TEXT_ADAPTER';
export const AnnotationAdapterToken = 'ANNOTATION_ADAPTER';